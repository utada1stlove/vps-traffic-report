services:
  central:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    volumes:
      - central-data:/var/lib/vps-central

volumes:
  central-data:
